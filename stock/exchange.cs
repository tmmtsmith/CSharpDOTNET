// initial

public static bool FoEx(string exch)
{

  string forex = ".";
  bool check = exch.Contains(Ger);
  return check;

}

// in prog

bool status = Class.FoEx(sym);
if (status)
{
  // check exchanges
}
